const finalParticipants = ["tyler", "Donald", "Dr", "Nasha", "Bob"];

const annonuncements = finalParticipants.map((member) => {
    return member + " joined the contest.";
});

console.log(annonuncements);


